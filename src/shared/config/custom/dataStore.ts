/**
 * Custom (X10000) DataStore Configuration
 * 
 * X10000 使用 API 后端，不需要链上 DataStore 键
 * 此文件用于保持配置结构的一致性
 * 
 * 如果需要定义 X10000 特定的配置常量，可以在这里添加
 */

// X10000 使用 API 后端，不需要 DataStore 键
// 如果将来需要定义 X10000 特定的配置，可以在这里添加

